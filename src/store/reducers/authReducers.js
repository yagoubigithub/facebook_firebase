const initStat = {

}
const authReducer = (state = initStat, action) =>{

    switch (action.type) {
        case 'display':
            return state;
           
    
        default:
            return state;
            
    }
    
}
export default authReducer;